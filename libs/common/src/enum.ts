export enum Environment {
  Local = 'local',
  Development = 'dev',
  Production = 'prod',
}

export enum LogLevel {
  Debug = 'debug',
  Error = 'error',
  Log = 'log',
  Verbose = 'verbose',
  Warn = 'warn',
}

export enum SortType {
  asc = 'ASC',
  desc = 'DESC',
}

export enum TypeEvent {
  AUTH = 'AUTH_',
  BCE = 'BCE_',
  HIGH_LOW = 'HIGH_LOW_',
}
