export enum IS_ACTIVE_CAMPAIGN {
  INACTIVE = 0,
  ACTIVE = 1,
}

export enum IS_SYSTEM {
  FALSE = 0,
  TRUE = 1,
}

export enum STATUS_CAMPAIGN {
  RUNNING = 0,
  EMPTY_MONEY = 1,
  ENDED = 2,
  INACTIVE = 3,
}
